name: Publish Skills to ClawHub

on:
  push:
    branches: [main]
    paths:
      - 'skills/**'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install ClawHub CLI
        run: npm install -g clawhub

      - name: Login to ClawHub
        run: clawhub login --token ${{ secrets.CLAWHUB_TOKEN }}

      - name: Publish china-hotel-price-compare
        run: |
          clawhub skill publish ./skills/china-hotel-price-compare \
            --owner travel-skills \
            --slug china-hotel-price-compare \
            --name "酒店比价" \
            --version 1.2.0 \
            --changelog "Chinese display name for better search discoverability"

      - name: Publish china-flight-price-compare
        run: |
          clawhub skill publish ./skills/china-flight-price-compare \
            --owner travel-skills \
            --slug china-flight-price-compare \
            --name "机票比价" \
            --version 1.2.0 \
            --changelog "Chinese display name for better search discoverability"
